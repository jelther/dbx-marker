mode: ContinuousDeployment
next-version: 1.0.0
branches:
  main:
    regex: main
    mode: ContinuousDeployment
    tracks-release-branches: true
    increment: Patch  # Automatically increment patch numbers on commits to main
ignore:
  sha: []